{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/spa",
    "framework": "vite",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}